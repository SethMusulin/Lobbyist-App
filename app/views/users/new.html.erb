<div class="users-sign-in">
<h2>Register</h2>
<%= form_for @user do |f| %>
    <% if @user %>
        <% if @user.errors %>
            <% @user.errors.full_messages.each do |message| %>
                <p class="error"><%= message %></p>
            <% end %>
        <% end %>
    <% end %>


    <%= f.label :email, class: "control-label"%>
   <p> <%= f.text_field :email, autofocus:true, class: "form-control"%></p>

    <%= f.label :password, class: "form-label" %>
    <p><%= f.password_field :password, class: "form-control" %></p>

    <%= f.label :password_confirmation, class: "form-label" %>
    <p> <%= f.password_field :password_confirmation, class: "form-control" %> </p>

    <%= f.submit class: "form-button", class: "btn btn-large btn-primary"  %>

<%end%>
</div>